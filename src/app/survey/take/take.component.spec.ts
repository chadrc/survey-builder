import {TakeComponent} from './take.component';

describe('TakeComponent', () => {
  let component: TakeComponent;

  beforeEach(() => {
    component = new TakeComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
