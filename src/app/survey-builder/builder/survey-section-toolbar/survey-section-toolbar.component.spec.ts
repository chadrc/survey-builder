import {SurveySectionToolbarComponent} from './survey-section-toolbar.component';

describe('SurveySectionToolbarComponent', () => {
  let component: SurveySectionToolbarComponent;

  beforeEach(() => {
    component = new SurveySectionToolbarComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
