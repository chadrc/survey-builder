import {SectionPanelsComponent} from './section-panels.component';

describe('SectionPanelsComponent', () => {
  let component: SectionPanelsComponent;

  beforeEach(() => {
    component = new SectionPanelsComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
