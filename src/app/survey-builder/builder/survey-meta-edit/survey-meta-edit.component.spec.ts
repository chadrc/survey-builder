import {SurveyMetaEditComponent} from './survey-meta-edit.component';

describe('SurveyMetaEditComponent', () => {
  let component: SurveyMetaEditComponent;

  beforeEach(() => {
    component = new SurveyMetaEditComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
